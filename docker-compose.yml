version: '3.8'

services:
  finance-app:
    build:
      context: .
      args:
        - REACT_APP_SUPABASE_URL=${REACT_APP_SUPABASE_URL}
        - REACT_APP_SUPABASE_ANON_ID=${REACT_APP_SUPABASE_ANON_ID}
    ports:
      - "3000:80"
    restart: always
