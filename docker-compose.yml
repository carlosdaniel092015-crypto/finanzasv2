version: '3.8'

services:
  finance-app:
    build:
      context: .
      args:
        - VITE_SUPABASE_URL=${VITE_SUPABASE_URL}
        - VITE_SUPABASE_ANON_ID=${VITE_SUPABASE_ANON_ID}
    ports:
      - "3000:80"
    restart: always
